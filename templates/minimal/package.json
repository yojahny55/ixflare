{
  "name": "{{projectName}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ix dev",
    "build": "ix build",
    "deploy": "ix deploy",
    "test": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "ixflare": "^0.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vite-plugin-ixflare": "^0.0.1",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
